# pyng

a minimalist python interface for ngspice that automates circuit value change in simulations

## dependencies

- *ngspice* that can be invoked directly in shell like "ngspice -r out.raw -b netlist_file"
